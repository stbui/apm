import React, { useRef, useEffect } from 'react';
import {
    Widget,
    TabbedPane,
    Toolbar,
    ToolbarButton,
    Icon,
    Checkbox,
} from '../../components';

export default () => {
    return <div class="widget vbox network-waterfall-view"></div>;
};
