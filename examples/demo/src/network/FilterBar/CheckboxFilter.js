import { h } from '../../core';
import { Checkbox } from '../../components';

export default () => (
    <div className="filter-checkbox-filter">
        <Checkbox label="Hide data URLs" />
    </div>
);
