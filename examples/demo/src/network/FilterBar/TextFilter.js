import React from 'react';
import { TextPrompt } from '../../components';

export default () => (
    <div className="filter-text-filter">
        <span style={{ display: 'inline-block' }}>
            <TextPrompt />
        </span>
    </div>
);
